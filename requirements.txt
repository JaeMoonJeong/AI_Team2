# ===================================================================
# [AI] 헬스잇(Health Eat) 프로젝트 (팀 2조) - Python 3.11 기반 
#
# 팀원 공통 환경 설치 방법: (가상환경 활성화 후)
# # pip install -r requirements.txt
# ===================================================================

# --- 1. 핵심 AI/ML (PyTorch) ---
# ※주의: PyTorch는 GPU(CUDA) 버전에 맞게 설치해야 함. 두개 중에 지원님 주용님께 맞춰 진행.

# (예: CUDA 11.8 사용 시)
# torch==2.8.0+cu126
# torchvision==0.23.0+cu126
# torchaudio==2.1.0+cu118
# (CPU 전용일 경우 아래 주석 해제)
# torch==2.8.0
# torchvision==0.23.0
# torchaudio==2.1.0

ultralytics==8.3.219

# --- 2. 데이터 핸들링 ---
pandas==2.2.2
numpy==2.0.2

# --- 3. 이미지 처리 (전처리, 증강) ---
opencv-python==4.12.0.88
Pillow==10.1.0

# --- 4. 시각화 및 유틸리티 ---
matplotlib==3.10.0
tqdm==4.67.1       # 학습 진행률 표시 (Progress Bar)
kaggle==1.7.4.5     # 캐글 API (데이터 다운로드, 제출)

# --- 5. 노트북 환경 (EDA, 실험용) ---
jupyterlab==4.0.8
notebook==7.0.6

# --- 6. (선택) 코드 품질/협업 도구 ---
# 팀의 코드 스타일을 통일하고 오류를 사전에 방지합니다.
black==23.11.0      # 포맷터 (코드 스타일 강제 통일)
flake8==6.1.0       # 린터 (문법/스타일 오류 검사)