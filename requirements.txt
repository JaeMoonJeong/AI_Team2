# ===================================================================
# [AI] 헬스잇(Health Eat) 프로젝트 (팀 2조) - Python 3.11 기반 
#
# 팀원 공통 환경 설치 방법: (가상환경 활성화 후)
# # pip install -r requirements.txt
# ===================================================================

# --- 1. 핵심 AI/ML (PyTorch) ---
# (예: CUDA 11.8 사용 시)
# torch==2.1.0+cu118
# torchvision==0.16.0+cu118
# torchaudio==2.1.0+cu118
# (CPU 전용일 경우 아래 주석 해제)
# torch==2.1.0
# torchvision==0.16.0
# torchaudio==2.1.0
# (pip freeze에서 추출한 버전 - 재문)
ultralytics==8.3.219
matplotlib==3.10.0
opencv-python-headless==4.10.0.84

# --- 2. 전처리 및 유틸리티 ---
# (conda로 설치되어 버전 번호가 @ file...로 표시됨)
# (버전 명시 없이 설치 권장)
PyYAML
tqdm
numpy
pandas
Pillow